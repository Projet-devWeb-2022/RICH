{% extends './baseUser.html.twig' %}



{% block title %}Hello PackController!{% endblock %}

{% block content %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <section class="title"> Nos destinations </section>
    <!-- Pour afficher l'ensemble des produits -->
{#
    {% for pack in listePack %}

        <p> Choissisez votre Pack : </p>

        <div class="block">
            <div class="container">
                <div class="columns">
                    <!-- one columns -->
                    <div class="column">
                        <div class="box">
                            <!-- <img class="" src="{ destination.image }}">  -->
                            <h2 class="subtitle has-text-link">{{ pack.nomPack }} </h2>
                            <h2 class="subtitle has-text-link is-4">{{ pack.destination }} </h2>
                            <p> {{ pack.nbJour }} adultes - </p> <p> {{ pack.nbJour }} jours </p></br>
                            <button  onclick=""> {{ pack.prix }} </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% endfor %}
#}
    </section>
</div>

    {% endblock %}
