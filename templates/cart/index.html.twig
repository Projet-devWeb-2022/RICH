<!DOCTYPE html>

<title>Hello CartController!</title>

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    <h1>Voici votre panier ✅</h1>

    This friendly message is coming from:
    <ul>
        {% if items | length > 0 %}
            <table>
                <thead>
                    <tr>
                        <th> destination </th>
                        <th> quantity </th>
                        <th> identifiant </th>
                    </tr>
                </thead>
                <tbody>
                {% for item in items %}
                <tr>
                    <td> {{ item.pack.name }}</td>
                    <td> {{ item.quantity}}</td>
                    <td> {{ item.pack.id }}</td>
                    <td>
                        <button> trash </button>
                    </td>
                </tr>
                {% endfor %}
                </tbody>
            </table>
        {% else %}
            <p> Votre panier est vide </p>

        {% endif %}
    </ul>
</div>
{% endblock %}
