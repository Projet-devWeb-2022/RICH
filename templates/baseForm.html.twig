    <!DOCTYPE html>
    <html lang="fr">
        <head>
            <meta charset="UTF-8">
            <title>{% block title %}{% endblock %}</title>
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
            <link rel="stylesheet" href="{{ asset('../node_modules/bulma/css/bulma.css') }}">
            <link rel="stylesheet" href="{{ asset('../node_modules/bulma/css/bulma-rtl.css') }}">
            <link rel="stylesheet" href="{{ asset('../node_modules/bulma/bulma.sass') }}">
            <link rel="stylesheet" href="{{ asset('../node_modules/bulma/bulma.sass') }}">
            <link rel="stylesheet" href="{{ asset('../assets/styles/formStyle.css') }}">
            {%- block stylesheets -%}
                <link rel="stylesheet" href="{{ absolute_url(asset('build/Bulma.css')) }}"/>
                {{ encore_entry_link_tags('Bulma') }}
            {%- endblock -%}

            <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
            {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
            {#{% block stylesheets %}
                {{ encore_entry_link_tags('app') }}
            {% endblock %}#}

            {% block javascripts %}
                {{ encore_entry_script_tags('app') }}
            {% endblock %}
        </head>
        <body class="has-background-grey-lighter">
            <div id="formBgd">
                <img src="{{ asset('../assets/img/logo_noir.png') }}">
                <section  class="has-background-white">
                    {% block content %}
                    {% endblock %}
                </section>
            </div>
        </body>
    </html>
