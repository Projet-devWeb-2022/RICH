{% extends 'baseUser.html.twig' %}



{% block title %}Hello DestinationPageController!{% endblock %}

{% block content %}
<style>
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <section class="title"> Nos destinations </section>

    <a href="{{ path('cart') }}">
        <button> Afficher mon panier </button>
    </a>

    <!-- Pour afficher l'ensemble des produits -->
    {% for destination in listDestination %}

        <p> Choissisez votre destination : </p>

        <div class="block">
            <div class="container">
                <div class="columns">
                    <!-- one columns -->
                    <div class="column">
                        <div class="box">
                            <!-- <img class="" src="{ destination.image }}">  -->
                            <h2 class="subtitle has-text-link">{{ destination.city }} </h2>
                            <h2 class="subtitle has-text-link is-4"> {{ destination.contry.name }} </h2>
                            <p> {{ destination.details }} {{destination.id}}</p>   </br>
                            <a href="{{ path('cart_add', {'id':destination.id})}}">
                                <button> selectionner </button>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% endfor %}

</section>

{% endblock %}
