{% extends 'user.html.twig' %}
        {% block content %}

            <div class="columns">
                <div class="column">
                    <h1>Nom</h1>
                </div>
                <div class="column">
                    <p>{{app.user.name}}</p>
                </div>
            </div>
            <div class="columns">
                <div class="column">
                    <h1>Prénom</h1>
                </div>
                <div class="column">
                    <p>{{app.user.surname}}</p>
                </div>
            </div>
            <div class="columns">
                <div class="column">
                    <h1>Adresse</h1>
                </div>
                <div class="column">
                    <p>{{app.user.address}}</p>
                </div>
            </div>

            
        {% endblock %}